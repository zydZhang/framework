build:
    environment:
        php:
            version: 7.1.9

    cache:
        directories:
            - ~/cphalcon

    dependencies:
        override:
            - composer install --prefer-source --no-interaction
        after:
            - bin/install-phalcon.sh 3.2.x
            - php -r "echo \Phalcon\Version::get();"